@extends('layouts.admin')

@section('content')
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
    @if(session('success'))
        <div class="mb-4 rounded border border-green-800 bg-green-900/30 px-4 py-2 text-green-300">{{ session('success') }}</div>
    @endif

    <div class="mb-6">
        <h2 class="text-2xl font-bold text-white mb-2">Business Settings</h2>
        <p class="text-gray-400">Configure {{ config('app.name', 'ClientBridge') }} for your business needs</p>
    </div>

    <form method="POST" action="{{ route('admin.business_settings.update') }}" class="space-y-8">
        @csrf
        @method('PATCH')

        <x-admin-layout>
    <div class="p-6">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-2xl font-bold text-gray-100 mb-6">Business Settings</h1>

            <form action="{{ route('admin.business_settings.update') }}" method="POST" class="space-y-6">
                @csrf
                @method('PUT')

                        <!-- Branding & Identity -->
                <div class="bg-gray-900 rounded-lg p-6 border border-gray-700">
                    <h2 class="text-xl font-semibold text-gray-100 mb-4">Branding & Identity</h2>
                    <p class="text-sm text-gray-400 mb-4">Configure your business name and company branding.</p>

                    <div class="space-y-4">
                        <div>
                            <label for="app_name" class="block text-sm font-medium text-gray-300 mb-1">Application Name</label>
                            <input id="app_name" type="text" name="app_name" value="{{ $settings['app_name'] }}" class="block w-full rounded-md border border-gray-700 bg-gray-800 text-gray-100 shadow-sm focus:border-blue-500 focus:ring-blue-500">
                            <p class="text-xs text-gray-500 mt-1">Appears in page titles and navigation. Syncs to <code class="bg-gray-700 px-1 rounded">APP_NAME</code> in .env file.</p>
                        </div>

                        <div>
                            <label for="company_name" class="block text-sm font-medium text-gray-300 mb-1">Company Name</label>
                            <input id="company_name" type="text" name="company_name" value="{{ $settings['company_name'] }}" class="block w-full rounded-md border border-gray-700 bg-gray-800 text-gray-100 shadow-sm focus:border-blue-500 focus:ring-blue-500" disabled>
                            <p class="text-xs text-yellow-500 mt-1">⚠️ Currently reads from <code class="bg-gray-700 px-1 rounded">BUSINESS_COMPANY_NAME</code> in .env file, not this database field. This setting will be re-enabled in a future update.</p>
                        </div>
                    </div>
                </div>

                        <!-- Theme Colors - NOT IMPLEMENTED -->
                <div class="bg-gray-900 rounded-lg p-6 border border-red-900/50">
                    <h2 class="text-xl font-semibold text-gray-100 mb-4">Theme Colors</h2>
                    <div class="bg-red-900/20 border border-red-800 rounded-lg p-4">
                        <div class="flex items-start">
                            <svg class="h-5 w-5 text-red-400 mt-0.5 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                            </svg>
                            <div>
                                <h3 class="text-sm font-medium text-red-300 mb-1">Feature Not Implemented</h3>
                                <p class="text-sm text-red-200/80">
                                    Theme colors are currently stored in the database but not applied anywhere in the application. 
                                    This feature requires implementation of CSS variable injection or Tailwind config updates.
                                </p>
                                <p class="text-xs text-red-300/60 mt-2">
                                    To customize colors now, edit the values in <code class="bg-gray-700 px-1 rounded">config/business.php</code> 
                                    and corresponding .env variables (<code class="bg-gray-700 px-1 rounded">BUSINESS_PRIMARY_COLOR</code>, etc.).
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Google Workspace - NOT IMPLEMENTED -->
                <div class="bg-gray-900 rounded-lg p-6 border border-yellow-900/50">
                    <h2 class="text-xl font-semibold text-gray-100 mb-4">Google Workspace Domain Restriction</h2>
                    <div class="bg-yellow-900/20 border border-yellow-800 rounded-lg p-4">
                        <div class="flex items-start">
                            <svg class="h-5 w-5 text-yellow-400 mt-0.5 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                            </svg>
                            <div>
                                <h3 class="text-sm font-medium text-yellow-300 mb-1">Feature Not Implemented</h3>
                                <p class="text-sm text-yellow-200/80">
                                    Google Workspace domain restriction is stored but not enforced during authentication. 
                                    This requires implementation in the Google OAuth callback to validate user email domains.
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

        <section class="rounded-lg border border-gray-800 bg-gray-900/50 p-6">
            <h3 class="text-lg font-semibold mb-4 text-gray-200">Google Workspace</h3>
            <div class="mb-4">
                <label for="google_workspace_domain" class="block text-sm font-medium text-gray-300 mb-1">Allowed domain (optional)</label>
                <input id="google_workspace_domain" type="text" name="google_workspace_domain" value="{{ $settings['google_workspace_domain'] }}" placeholder="example.com" class="block w-full rounded-md border border-gray-700 bg-gray-800 text-gray-100 shadow-sm focus:border-blue-500 focus:ring-blue-500">
                <span class="text-xs text-gray-400">If set, only users from this domain can sign in via Google.</span>
            </div>
        </section>

        <div class="pt-2">
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded-md">
                <i class="fas fa-save mr-2"></i>Save Settings
            </button>
        </div>
    </form>
</div>

<script>
// Sync color picker with hex value display
document.querySelectorAll('input[type="color"]').forEach(colorInput => {
    const hexDisplay = colorInput.nextElementSibling;
    
    colorInput.addEventListener('input', (e) => {
        if (hexDisplay && hexDisplay.type === 'text') {
            hexDisplay.value = e.target.value.toUpperCase();
        }
    });
});
</script>
@endsection
